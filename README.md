The contact list format is csv, the default header is:
phoneBookName, contactName, contactNumber


1) The file contacts.csv is stored under target
2) All new data will be removed when doing mvn clean


